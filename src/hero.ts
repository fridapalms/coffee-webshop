export function products() {
  const implementProducts = document.getElementById("products");

  const heroProducts = ["./img/image 4.png", "./img/Vector.png"];

  for (let i = 0; i < heroProducts.length; i++) {
    const coffee = heroProducts[i];
  }
}
